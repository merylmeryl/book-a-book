import Button from "./Button";
import buttonStyles from "../../styles/Button.module.css";

const ButtonCheckout = () => {
  return (
    <div>
      <Button className="" text="Check Out"></Button>
    </div>
  );
};

export default ButtonCheckout;
